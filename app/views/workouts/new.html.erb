
  <%= form_for @workout, url: user_workouts_path do |f| %>
    <h2>Add Workout</h2>
    <%= f.label :name, "Describe Your Workout: " %>
    <%= f.text_field :name %>
    <%= f.label :date, "Date of Workout:" %>
    <%= f.date_select :date %>
    <%= f.submit 'Submit' %>
  <% end %>