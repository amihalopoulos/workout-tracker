<%= form_for current_user.following.find_by(user_id: @user.id), html: { method: :delete }, remote: true do |f| %>
  <%= f.submit "Unfollow", id: "unfollow_button", class: "button edit_button" %>
<% end %>