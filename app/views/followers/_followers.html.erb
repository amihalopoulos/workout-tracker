<div class="content">
  <div class="follow_header">
    <h1>Followers</h1>
  </div>
  <% @user.followers.each do |follower| %>
    <p><%= link_to follower.user.name, user_path(follower.user) %></p>
  <% end %>
</div>
