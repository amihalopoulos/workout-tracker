<%= form_for current_user.following.build, remote: true do |f| %>
  <div><%= hidden_field_tag :user_id, @user.id %></div>
  <%= f.submit "Follow", id: "follow_button", class: "button edit_button" %>
<% end %>